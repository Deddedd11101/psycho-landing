/**
 * Справочник направлений работы.
 * Единственный источник правды: файл импортируется и фронтендом (алиас @shared),
 * и сервером — чтобы подписи в анкете и в сообщении психологу всегда совпадали.
 *
 * Важно: файл не должен импортировать ничего, чтобы его можно было безопасно
 * подключать в браузерный бандл.
 */

export interface TopicOption {
  /** Машинный ключ — именно он уходит на сервер. */
  key: string;
  /** Подпись для интерфейса и сообщений. */
  label: string;
  /** Короткое пояснение под чекбоксом. */
  hint: string;
}

export const TOPIC_OPTIONS: TopicOption[] = [
  { key: 'relationships', label: 'Отношения', hint: 'Партнёр, семья, родители, сложные расставания' },
  { key: 'self_esteem', label: 'Самооценка и уверенность', hint: 'Самокритика, страх ошибки, зависимость от чужого мнения' },
  { key: 'anxiety', label: 'Тревожные состояния', hint: 'Постоянное напряжение, страхи, беспокойный сон' },
  { key: 'patterns', label: 'Повторяющиеся сценарии', hint: 'Одни и те же ситуации в отношениях и работе' },
  { key: 'burnout', label: 'Выгорание и усталость', hint: 'Работа, из которой не получается выйти' },
  { key: 'apathy', label: 'Подавленность и апатия', hint: 'Нет сил и радости, всё даётся через силу' },
  { key: 'boundaries', label: 'Границы и потеря себя', hint: 'Сложно отказывать, теряете себя рядом с другими' },
  { key: 'loss', label: 'Утрата и тяжёлые события', hint: 'Переживания, с которыми трудно справиться в одиночку' },
];

/** Возвращает подпись направления по ключу. Неизвестный ключ отдаём как есть. */
export function topicLabel(key: string): string {
  return TOPIC_OPTIONS.find((option) => option.key === key)?.label ?? key;
}

/** Подписи для поля «пол». */
export const GENDER_LABELS: Record<string, string> = {
  female: 'Женский',
  male: 'Мужской',
  other: 'Другой / не хочу указывать',
};

/** Подписи для формата встречи. */
export const FORMAT_LABELS: Record<string, string> = {
  online: 'Онлайн (видеосвязь)',
  offline: 'Очно в кабинете',
};
